const DestructProperties = ({name, age, title}) => {
  return (
    <div>
        <h1>Destructuring Properties</h1>
        <h2>Name: {name}</h2>
        <h2>Age: {age}</h2> 
        <h2>Title: {title}</h2> 
    </div>
  )
}

export default DestructProperties